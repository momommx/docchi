public/users/:id/favorites

<div class="container">
    <div class="mt-4">
        <h4>いいねした投稿一覧</h4>
        <%= render partial: 'topics', locals: { topics: @favorite_topics, user: @user } %>
    </div>
</div>

